<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".ui.receipt.ReceiptPlanActivity">

    <include layout="@layout/common_toolbar" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/receipt_calendar_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal">

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/selector_tabs_bg"
            android:drawableTop="@drawable/ic_receipt_warehouse"
            android:drawablePadding="@dimen/dp5"
            android:paddingHorizontal="@dimen/dp28"
            android:paddingVertical="@dimen/dp4"
            android:text="大仓"
            android:textColor="@color/black" />

        <View
            android:layout_width="@dimen/dp43"
            android:layout_height="1dp" />

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/selector_tabs_bg"
            android:drawableTop="@drawable/ic_receipt_store"
            android:drawablePadding="@dimen/dp5"
            android:paddingHorizontal="@dimen/dp28"
            android:paddingVertical="@dimen/dp4"
            android:text="他店"
            android:textColor="@color/black" />

    </LinearLayout>


</LinearLayout>